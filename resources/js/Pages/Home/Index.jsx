import Layout from '../../Components/Layout';
import Carousel from './Carousel';
import Hero from './Hero';

const Home = () => {
    return (
        <Layout>
            <Hero />
            <Carousel />
        </Layout>
    );
};

export default Home;
